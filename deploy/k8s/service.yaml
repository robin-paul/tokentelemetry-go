apiVersion: v1
kind: Service
metadata:
  name: tokentelemetry-hub
  namespace: telemetry
  labels:
    app: tokentelemetry-hub
spec:
  type: ClusterIP
  selector:
    app: tokentelemetry-hub
  ports:
    - port: 8000
      targetPort: 8000
      name: http
